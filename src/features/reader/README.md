# Reader Feature

Owns the PDF reading surface: reader layout, page display, page navigation, and viewport state.

Browser-only PDF and canvas work should live in client components or client-side hooks inside this feature.
