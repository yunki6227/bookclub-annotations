# Annotations Feature

Owns annotation domain types, drawing tool concepts, normalized coordinate conversion, serialization, and local persistence.

Annotations should be stored as vector stroke data using `0` to `1` page coordinates.
